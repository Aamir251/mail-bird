import { sidebarLinks } from "./constants"

const SideBar = () => {
  return (
    <aside>
      hello
      {
        // sidebarLinks.map(link => <SidebarLink key />)
      }
    </aside>
  )
}

export default SideBar
